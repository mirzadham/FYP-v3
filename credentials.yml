# Credentials Configuration
# Academic Advisor Chatbot - UPM

# Telegram channel configuration
# Replace placeholders with your actual values
telegram:
  access_token: "YOUR_TELEGRAM_BOT_TOKEN"
  verify: "YOUR_BOT_USERNAME"
  webhook_url: "YOUR_PUBLIC_URL/webhooks/telegram/webhook"

# REST channel for testing (always enabled for debugging)
rest:

# Socket.IO for Rasa X interface (optional)
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true
